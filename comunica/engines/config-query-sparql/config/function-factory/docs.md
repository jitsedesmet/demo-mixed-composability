mediator file here needs to tell of itself that it has the same `id`
as the original (so references do not break).

Within config-default, all configs are brought together. there, we should import this mediator file instead of the Comunica base one.
