name: Build-and-push

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    uses: ./.github/workflows/build-might-push.yml
    with:
      push: true
      tag: development
    permissions:
      contents: read
      packages: write
